def maxindex(data_str: dict|list|tuple)->int:
    
    for i in data_str:
        
        index = data_str.index(i)
        
        maximum = 0
        
        if index > maximum:
            
            maximum = index
        
        else:
            
            continue
        
        return maximum
